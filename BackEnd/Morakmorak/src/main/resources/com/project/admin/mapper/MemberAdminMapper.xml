<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.admin.mapper.MemberAdminMapper">
<!-- 모든 회원 조회 -->
	<select id="getUsers"
		resultType="map">
		SELECT no, name, email, phone, to_date(reg_date, 'yyyy-mm-dd'), gender, nickname, self_pr, black
		FROM member ORDER BY no DESC
	</select>
</mapper>