<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.admin.mapper.ReviewAdminMapper">
	<!-- 후기 목록 조회 -->
    <select id="getReviews" resultType="com.project.admin.model.ReviewAdmin">
        SELECT * FROM review r left join member m on r.no = m.no ORDER BY r.no DESC
    </select>

    <!-- 후기 블라인드 처리 -->
    <update id="toggleBlind">
        UPDATE review SET isblacked = 'Y' WHERE no = #{no}
    </update>
    
    <!-- 후기 블라인드 처리 Y->N -->
    <update id="toggleBlindYN">
        UPDATE review SET isblacked = 'N' WHERE no = #{no}
    </update>
	
</mapper>