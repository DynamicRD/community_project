<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.admin.mapper.ReviewAdminMapper">
	<!-- 후기 목록 조회 -->
    <select id="getReviews" resultType="com.project.admin.model.ReviewAdmin">
        SELECT * FROM review WHERE hidden = FALSE ORDER BY id DESC
    </select>

    <!-- 특정 후기 조회 -->
    <select id="getReviewById" resultType="com.project.admin.model.ReviewAdmin">
        SELECT * FROM review WHERE id = #{id}
    </select>

    <!-- 후기 추가 -->
    <insert id="addReview" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review (review_title, nickname, img_url, content, star, isblacked)
        VALUES (#{review_title}, #{nickname}, #{img_url}, #{content}, #{star}, 'N')
    </insert>

    <!-- 후기 수정 -->
    <update id="updateReview">
        UPDATE review 
        SET review_title = #{review_title}, content = #{content}, img_url = #{img_url}, rating = #{star} 
        WHERE id = #{id}
    </update>

    <!-- 후기 삭제 -->
    <delete id="deleteReview">
        DELETE FROM review WHERE no = #{no}
    </delete>

    <!-- 후기 블라인드 처리 -->
    <update id="toggleBlind">
        UPDATE review SET isblacked = !isblacked WHERE no = #{no}
    </update>
	
</mapper>