<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.announcements.mapper.FaqMapper">
	<select id="listFaq" resultType="java.util.Map">
		SELECT FAQ_NO, FAQ_TITLE, CONTENT, TO_CHAR(REG_DATE, 'YYYY-MM-DD') AS
		REG_DATE FROM FAQ ORDER BY FAQ_NO DESC
	</select>

	<insert id="insertFaq">
		INSERT INTO FAQ (FAQ_NO, FAQ_TITLE, CONTENT, REG_DATE) VALUES
		(FAQ_SEQ.NEXTVAL, #{n_title}, #{content}, SYSDATE)
	</insert>
</mapper>